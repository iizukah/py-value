# 開発環境用 環境変数テンプレート
# コピーして .env を作成し、値を設定すること。.env はリポジトリにコミットしない。

# データソース: lowdb | firestore
DATA_SOURCE=lowdb

# Lowdb 用 JSON を置くディレクトリ（DATA_SOURCE=lowdb のとき）
LOWDB_PATH=./data

# 管理 API のクエリ key (?key=xxx)。本番は Firebase Console 等で設定すること。
ADMIN_KEY=your-admin-key-here

# Node 環境
NODE_ENV=development
