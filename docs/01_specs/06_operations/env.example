# 開発環境用 環境変数テンプレート（INFRA-01 §4.2, §6）
# コピーして .env を作成し、値を設定すること。.env はリポジトリにコミットしない。
# 詳細: docs/02_manuals/02_デプロイマニュアル.md §6

# データソース: lowdb | firestore
DATA_SOURCE=lowdb

# Lowdb 用 JSON を置くディレクトリ（DATA_SOURCE=lowdb のときのみ。本番 firestore では不要）
LOWDB_PATH=./data

# 管理 API のクエリ key (?key=xxx)。本番は人間が値を決め、Firebase Console 等で設定すること。
# 実装は ADMIN_API_KEY を優先し、未設定時は ADMIN_KEY を参照。E2E で管理画面を通す場合は ADMIN_KEY=test-admin-key にするとスキップされない。
ADMIN_KEY=your-admin-key-here

# Node 環境
NODE_ENV=development

# --- 本番シード投入時のみ（ローカルで node scripts/seed-firestore.mjs を実行する場合）---
# サービスアカウントキー JSON の絶対パス。未設定の場合は gcloud auth application-default login 等の ADC を使用。
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
