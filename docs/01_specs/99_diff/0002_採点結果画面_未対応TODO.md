# 0002 採点結果画面（SC-003）未対応 To-Do

## 日付

2026-02-07

## 概要

採点結果画面（docs/.drafts/mock.html の `#panel-result`）について、以下の UI 変更を実施したが **表示に反映されず**、変更は中断した。今後の対応用に To-Do として 99_diff に記録する。

---

## 未対応 To-Do 一覧

| No. | 内容 | 備考 |
|-----|------|------|
| 1 | **タイルの横幅を画面の約50%にする** | 左右マージンを十分取り、タイル幅を現状の約半分（画面の50%程度）にする。CSS で `max-width: 50%` / `width: 50%` / `align-items: center`（親）などを指定しても表示が変わらなかった。原因未特定。 |
| 2 | **「Retry」ボタンをタイルの右下端に配置する** | 文言は「次の問題へ」→「Retry」に変更済み。位置をタイルの右下端にしたい。フッターに `justify-content: flex-end` や `margin-left: auto` を指定してもボタンが左側に表示されたまま。原因未特定。 |

---

## 試したこと（記録用）

- `.result-layout` に `align-items: center` を追加
- `.panel-result .result-tile` に `max-width: 320px` / `max-width: 50%` / `width: 50%` などを指定
- タイルを包む `.result-tile-wrap` を追加し `display: flex; justify-content: center` で中央寄せ
- `#result-tile` の ID セレクタと `!important` で幅・フッターを強制 → **タイル自体が表示されなくなりロールバック**
- `.tile-footer-actions-vertical` の `width: 100%` を削除し `margin-left: auto` で右寄せを試行

---

## 関連ドキュメント ID

- UX-01 SC-003（採点結果画面）
- UX-02（該当コンポーネント）
- 参考: docs/.drafts/mock.html
